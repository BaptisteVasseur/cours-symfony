{% extends 'layout/admin.html.twig' %}

{% block page_title %}Nouvel utilisateur{% endblock %}

{% block content %}
    <div class="mb-6">
        <a href="{{ path('app_user_index') }}" class="inline-flex items-center gap-2 text-sm text-gray-500 hover:text-gray-900 transition">
            <i class="fa-solid fa-arrow-left text-xs"></i>
            Retour à la liste
        </a>
    </div>

    <div class="bg-white rounded-xl border border-gray-200 shadow-sm p-6 max-w-5xl">
        <h3 class="font-bold text-gray-900 mb-6">Créer un utilisateur</h3>
        {{ include('admin/user/partials/form.html.twig', { button_label: 'Créer l\'utilisateur' }) }}
    </div>
{% endblock %}
