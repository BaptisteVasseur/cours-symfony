<aside class="w-64 bg-gray-900 text-white flex flex-col justify-between hidden md:flex shrink-0">
    <div>
        <div class="p-5 border-b border-gray-800 flex items-center space-x-2 text-brand text-xl font-bold">
            <i class="fa-solid fa-house-chimney text-2xl"></i>
            <span class="text-white text-sm tracking-wider uppercase font-semibold">Espace Hôte</span>
        </div>
        <nav class="p-4 space-y-1">
            <a href="{{ path('app_host_dashboard') }}" class="flex items-center space-x-3 {{ app.request.attributes.get('_route') == 'app_host_dashboard' ? 'bg-gray-800 text-white' : 'text-gray-400 hover:bg-gray-800 hover:text-white' }} px-4 py-3 rounded-lg text-sm font-medium transition">
                <i class="fa-solid fa-chart-pie text-brand"></i>
                <span>Vue d'ensemble</span>
            </a>
            <a href="{{ path('app_host_property_index') }}" class="flex items-center space-x-3 {{ app.request.attributes.get('_route') starts with 'app_host_property' ? 'bg-gray-800 text-white' : 'text-gray-400 hover:bg-gray-800 hover:text-white' }} px-4 py-3 rounded-lg text-sm font-medium transition">
                <i class="fa-solid fa-house"></i>
                <span>Mes Annonces</span>
            </a>
            <a href="{{ path('app_host_booking_index') }}" class="flex items-center space-x-3 {{ app.request.attributes.get('_route') starts with 'app_host_booking' ? 'bg-gray-800 text-white' : 'text-gray-400 hover:bg-gray-800 hover:text-white' }} px-4 py-3 rounded-lg text-sm font-medium transition">
                <i class="fa-solid fa-credit-card"></i>
                <span>Réservations reçues</span>
            </a>
            <a href="{{ path('app_home') }}" class="flex items-center space-x-3 text-gray-400 hover:bg-gray-800 hover:text-white px-4 py-3 rounded-lg text-sm font-medium transition">
                <i class="fa-solid fa-arrow-left"></i>
                <span>Retour au site</span>
            </a>
        </nav>
    </div>

    <div class="p-4 border-t border-gray-800 flex items-center space-x-3">
        <div class="w-9 h-9 bg-brand text-white rounded-full flex items-center justify-center font-bold text-sm">
            {{ app.user ? app.user.email|first|upper : 'H' }}
        </div>
        <div>
            <p class="text-xs font-semibold text-white">{{ app.user ? app.user.email : '' }}</p>
            <span class="text-[10px] text-emerald-400 font-medium"><i class="fa-solid fa-circle text-[8px] mr-1"></i> Hôte</span>
        </div>
    </div>
</aside>
