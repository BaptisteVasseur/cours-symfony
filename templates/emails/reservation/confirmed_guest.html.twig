<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Réservation confirmée</title>
</head>
<body style="font-family: Arial, sans-serif; padding: 20px;">
    <h1 style="color: #27ae60;"> Réservation confirmée !</h1>
    
    <p>Bonjour <strong>{{ reservation.guest.profile.firstName|default('Voyageur') }}</strong>,</p>
    
    <p>Votre réservation est maintenant confirmée :</p>
    
    <div style="background: #f5f5f5; padding: 15px; border-radius: 10px; margin: 20px 0;">
        <h2 style="margin: 0 0 10px 0;">{{ reservation.property.title }}</h2>
        <p><strong>Dates :</strong> {{ reservation.checkinDate|date('d/m/Y') }} → {{ reservation.checkoutDate|date('d/m/Y') }}</p>
        <p><strong>Voyageurs :</strong> {{ reservation.guestsCount }}</p>
        <p><strong>Total :</strong> {{ reservation.totalPrice|number_format(2, ',', ' ') }} {{ reservation.currency|default('EUR') }}</p>
    </div>
    
    <p>
        <a href="{{ url('app_reservation_show', { id: reservation.id }) }}" style="background: #27ae60; color: white; padding: 10px 20px; text-decoration: none; border-radius: 5px;">
             Voir ma réservation
        </a>
    </p>
    
    <p style="color: #666; font-size: 12px; margin-top: 30px;">Cet email a été envoyé automatiquement, merci de ne pas y répondre.</p>
</body>
</html>