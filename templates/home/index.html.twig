{% extends 'layout/base.html.twig' %}

{% block body %}

    <!-- Filters -->
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
        <div class="flex items-center space-x-8 overflow-x-auto scrollbar-hide pb-4">
            <button class="flex items-center space-x-2 px-4 py-2 border border-gray-300 rounded-full hover:border-gray-900 transition-colors whitespace-nowrap">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 4a1 1 0 011-1h16a1 1 0 011 1v2.586a1 1 0 01-.293.707l-6.414 6.414a1 1 0 00-.293.707V17l-4 4v-6.586a1 1 0 00-.293-.707L3.293 7.293A1 1 0 013 6.586V4z"></path>
                </svg>
                <span class="text-sm font-medium">Filtres</span>
            </button>
            <button class="px-4 py-2 border border-gray-300 rounded-full hover:border-gray-900 transition-colors whitespace-nowrap">
                <span class="text-sm font-medium">Logements entiers</span>
            </button>
            <button class="px-4 py-2 border border-gray-300 rounded-full hover:border-gray-900 transition-colors whitespace-nowrap">
                <span class="text-sm font-medium">Séjours flexibles</span>
            </button>
            <button class="px-4 py-2 border border-gray-300 rounded-full hover:border-gray-900 transition-colors whitespace-nowrap">
                <span class="text-sm font-medium">WiFi</span>
            </button>
            <button class="px-4 py-2 border border-gray-300 rounded-full hover:border-gray-900 transition-colors whitespace-nowrap">
                <span class="text-sm font-medium">Piscine</span>
            </button>
            <button class="px-4 py-2 border border-gray-300 rounded-full hover:border-gray-900 transition-colors whitespace-nowrap">
                <span class="text-sm font-medium">Cuisine</span>
            </button>
            <button class="px-4 py-2 border border-gray-300 rounded-full hover:border-gray-900 transition-colors whitespace-nowrap">
                <span class="text-sm font-medium">Parking</span>
            </button>
            <button class="px-4 py-2 border border-gray-300 rounded-full hover:border-gray-900 transition-colors whitespace-nowrap">
                <span class="text-sm font-medium">Chauffage</span>
            </button>
        </div>
    </div>

    <!-- Properties Grid -->
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pb-12">
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
            {% for property in properties %}
                {% include 'parts/_property-card.html.twig' %}
            {% endfor %}
        </div>
    </main>

{% endblock %}
