<!DOCTYPE html>
<html lang="fr">
<head><meta charset="UTF-8"><title>Demande de réservation</title></head>
<body style="font-family: Arial, sans-serif; background:#f4f4f4; margin:0; padding:20px;">
<div style="max-width:600px; margin:0 auto; background:#fff; border-radius:8px; overflow:hidden;">
    <div style="background:#FF5A5F; padding:24px 32px;">
        <h1 style="color:#fff; margin:0; font-size:22px;">
            {% if recipient == 'host' %}Nouvelle demande de réservation{% else %}Demande envoyée{% endif %}
        </h1>
    </div>
    <div style="padding:32px;">
        {% set property = reservation.property %}
        {% set guest = reservation.guest %}
        {% set guestProfile = guest ? guest.profile : null %}

        {% if recipient == 'host' %}
            <p>Bonjour,</p>
            <p><strong>{{ guestProfile ? guestProfile.firstName ~ ' ' ~ guestProfile.lastName : guest.email }}</strong>
            souhaite réserver votre logement.</p>
        {% else %}
            <p>Bonjour {{ guestProfile ? guestProfile.firstName : 'cher voyageur' }},</p>
            <p>Votre demande de réservation a bien été envoyée à l'hôte. Vous serez notifié(e) dès qu'elle sera traitée.</p>
        {% endif %}

        <table style="width:100%; border-collapse:collapse; margin:24px 0;">
            <tr style="background:#f8f8f8;">
                <td style="padding:12px; font-weight:bold;">Logement</td>
                <td style="padding:12px;">{{ property ? property.title : '—' }}</td>
            </tr>
            <tr>
                <td style="padding:12px; font-weight:bold;">Arrivée</td>
                <td style="padding:12px;">{{ reservation.checkinDate|date('d/m/Y') }}</td>
            </tr>
            <tr style="background:#f8f8f8;">
                <td style="padding:12px; font-weight:bold;">Départ</td>
                <td style="padding:12px;">{{ reservation.checkoutDate|date('d/m/Y') }}</td>
            </tr>
            <tr>
                <td style="padding:12px; font-weight:bold;">Voyageurs</td>
                <td style="padding:12px;">{{ reservation.guestsCount }}</td>
            </tr>
            <tr style="background:#f8f8f8;">
                <td style="padding:12px; font-weight:bold;">Montant total</td>
                <td style="padding:12px;">{{ reservation.totalPrice|number_format(2, ',', ' ') }} {{ reservation.currency|default('EUR') }}</td>
            </tr>
        </table>

        {% if recipient == 'host' %}
            <p style="color:#666; font-size:13px;">Connectez-vous à votre espace hôte pour accepter ou refuser cette demande.</p>
        {% endif %}
    </div>
    <div style="background:#f8f8f8; padding:16px 32px; text-align:center; color:#999; font-size:12px;">
        © {{ "now"|date("Y") }} StayHub — Ce message est automatique, merci de ne pas y répondre.
    </div>
</div>
</body>
</html>
