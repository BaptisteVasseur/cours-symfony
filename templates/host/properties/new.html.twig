{% extends 'layout/host.html.twig' %}

{% block page_title %}Ajouter un logement{% endblock %}

{% block content %}
<div class="mb-6">
    <a href="{{ path('host_properties') }}" class="inline-flex items-center gap-2 text-sm text-slate-500 hover:text-slate-900 transition">
        <i class="fa-solid fa-arrow-left text-xs"></i>
        Retour à la liste
    </a>
</div>

<div class="bg-white rounded-xl border border-slate-200 shadow-sm p-6 max-w-5xl">
    <h3 class="font-bold text-slate-900 mb-6">Ajouter un logement sur Airbnb</h3>
    {{ include('host/properties/_form.html.twig', { button_label: 'Ajouter le logement' }) }}
</div>
{% endblock %}
