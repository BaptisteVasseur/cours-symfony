<form method="post" action="{{ path('app_listing_delete', {'id': listing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ listing.id) }}">
    <button class="btn">Delete</button>
</form>
