# Protection CSRF classique basée sur la session.
# Tous les formulaires (login, inscription, déconnexion ET les formulaires admin
# de rôle/suppression) utilisent le même mécanisme stocké en session : robuste,
# sans dépendance au JS Stimulus / cookie / Turbo.
framework:
    csrf_protection: true
